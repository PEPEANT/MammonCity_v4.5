# 배금도시 1 (V1) — 확정 구조 / 출시 대상

> 이게 **지금 만드는 게임**이다. 단순하게, 먼저 출시한다.
> 거대 설계(회귀 5엔딩 사다리·성형·마약·12단어 = `01_story-branches.md`)와
> "결핍이 메뉴를 낳는" 폰 시스템(= `배금도시2_설계_미래스펙.md`)은 **전부 V2로 보류.**

---

## 1. 로그라인 (회귀 제거됨)

**스물다섯, 평범한 고시원 청년이 연애가 하고 싶어 고백했다 차인다. 홧김에 주식·로또·도박에 손을 대고 — 그 한 번의 충동이 그를 어느 계급으로 떨어뜨리거나 끌어올린다.**

- **회귀 없음. 차트 보는 눈(능력) 없음.** 그냥 현실의 25살.
- 연애는 **동기**(고백→차임→빡침)일 뿐, **결과는 돈/계급(수저)으로 측정**한다.
- 톤: 현실적, 날것. 능력 판타지가 아니라 충동과 그 대가.

### 폐기/보류된 옛 설정 (V1에서 안 씀)
- ☠️ 26살 한서기·회귀·"분명 죽었었는데"·증권앱이 묘하게·차트 심리 읽기 → **삭제** (현실성 없는 판타지)
- ⏸️ 유민아 / 미애·경원 연애 엔딩 / 회귀 4막 사다리 / 성형·사기·마약 12단어 → **V2 보관**

---

## 2. 진행 구조 — 5주 / 진엔딩 4개

```
1주차 → 2주차 → 3주차 ──→ 4주차 ┬─ 분기 A ┬─ 5주차 → 진엔딩 ①
   (공통 빌드업, 직선)            │        └─ 5주차 → 진엔딩 ②
                                 └─ 분기 B ┬─ 5주차 → 진엔딩 ③
                                          └─ 5주차 → 진엔딩 ④
```

- **1~3주차**: 직선. 공통 빌드업 (고백→차임→첫 베팅까지).
- **4주차**: 2갈래 분기.
- **5주차**: 각 갈래가 또 갈라져 → **진엔딩 4개.**
- 원작(스크린샷의 프롤로그→1~4주차 연애 3엔딩)은 **시각 참고일 뿐**, 글은 이 방향.

---

## 3. 진엔딩 4개 — 수저 계급 (성공↔실패)

| 엔딩 | 성격 | 대략 조건 |
|------|------|-----------|
| **흙수저** | 실패/제자리. 빚·바닥, 변한 것 없음 | 도박 망함 / 소극·회피 |
| **은수저** | 소소한 성공. 평범하게 한 칸 올라감 | 절제 + 작은 수익 |
| **금수저** | 큰 성공. 부자가 됨 (단, 시스템에 갇힌 자) | 과감 + 운/판단이 맞음 |
| **🔒 숨겨진 진엔딩 — "시스템을 깨달은 개발자"** | 반전. 표면 실패가 진짜 해방으로 뒤집힘. **시뮬라크월드로 가는 문** | **흙수저 + 투자 실패** 루트 + 각성 플래그 |

### 🔒 숨겨진 진엔딩 — 확정 (작품의 척추)

금수저(돈 성공)로는 **절대 안 열린다.** 오직 **흙수저 + 투자 실패** 루트에서만 열리는 반전 엔딩.

**경로(각성 플래그로 추적):**
히키코모리로 시작 → 애니메이션·픽사 영화를 잔뜩 봄(상상력의 양분) → 개발·AI 대화에 몰두 →
투자를 통해 **세계 구조와 부조리를 파악** → 투자로 실패 → 그 바닥에서 **AI를 만남** → 다음 세계로 넘어감.

**의미:** 돈으로 정점에 오르는 게 아니라(그건 금수저=시스템에 갇힌 자), **돈에 실패하고 AI와 함께 세계를 만드는 창작자로 각성**한다. 표면적 실패(흙수저)가 진짜 해방으로 뒤집힌다.

**연결:** 이 엔딩이 곧 **배금도시(돈의 지옥) → 시뮬라크월드(AGI 이후)로 넘어가는 다리.** 첫 메시지의 세계관(배금도시→메가인터넷시티→시뮬라크월드)과 직결. 사실상 제작자 본인의 실제 경로(투자실패→AI→창작→세계관)를 게임의 진엔딩으로 박은 것.

**주제 격발:** *돈은 끝내 의미를 못 산다. 돈을 깨고 나온 자만이 다음 세계로 간다.*

> V1 구현 메모: 5주차 동안 돈(cash/debt/assets)만이 아니라 **각성 플래그**(예: `anime_watched`, `ai_dialog`, `dev_focus`, `invest_failed`, `hikikomori`)를 누적 추적. 흙수저 도달 + 각성 플래그 임계 충족 시 숨겨진 진엔딩으로 분기. 플래그 미충족 흙수저는 그냥 흙수저(실패) 엔딩.

---

## 4. 코드 반영 (scenes.js만 손댐, 엔진 안 건드림)

현재 `game/data/scenes.js` 상태:
- ✅ **라이브 경로**: `title → create → a1_wake → a1_bathroom/oversleep/… → p_end` (25살, 고시원, 마감조 알바, 폰 연출). **이걸 1주차 빌드업으로 이어쓴다.**
- ☠️ **삭제 대상(고아 코드)**: `p_card, p1~p6, p5, p6, c1, c1_a/b/c` (26살 회귀·유민아). 어디서도 안 불리는 죽은 씬.
- 🔧 **수정**: 타이틀 `kicker: '회귀 첫날 밤'` → 회귀 빼고 교체. `album`의 `cg_seogi_1`(회귀) / `cg_yumina_1`(유민아) 정리.

### 다음 작업 순서
1. 회귀 고아 블록 삭제 + 타이틀/앨범 회귀 흔적 제거 → `validate-story.js`로 깨진 데 확인.
2. `a1_*`(프롤로그/1주차 도입) 뒤로 **고백→차임** 장면 작성 (트리거).
3. **첫 베팅**(주식/로또/도박 선택) → 2주차.
4. 3주차 빌드업 → 4주차 2분기 → 5주차 4엔딩 스캐폴딩.
5. `flags`/`economy`(cash/debt/assets)로 엔딩 판정 (엔진에 이미 있음).

---

## 4-1. 1주차 — 구현 완료 (엄마 + 버스정류장 헌팅)

첫 사건으로 **시작 자본 + 가족 관계 + 분노**를 한 번에 정하는 장면.

```
1주차 기상 → 엄마 밥 발견 → 전화? ┬ 무시 → mother_missed (엄마돈 루트 잠김)
                                  └ 전화 ┬ 화낸다   → mother-1, anger+1
                                        ├ 수긍     → 안정(눌림)
                                        ├ 감사     → mother+1 (도움 루트)
                                        └ 돈 빌린다 → cash+500만, mother_debt, guilt+1, pressure+1
   → 버스정류장, 여자 → 헌팅? ┬ [직설] 번호 좀…  → 실패, anger+2 → 홧김에 베팅
                            ├ [진심] 후회할 것… → 성공, her+2 → "돈이 든다" pressure+1 → 베팅
                            └ 포기            → 후회, anger+1 → 베팅
   → 2주차 첫 베팅 (성공/실패 모두 도박으로 수렴)
```

- **헌팅은 결정론**: 대사가 결과를 확정 (직설=실패, 진심=성공). 운 없음.
- **엄마 돈 = 빚**: 공짜 보너스 아님. cash+500만에 mother_debt·guilt·pressure가 붙어 엔딩에 걸림.
- **분노/죄책감/압박 스탯**: 엔진 호감도(affection) 맵에 누적(+1씩)으로 저장 — `anger`, `guilt`, `pressure`, `mother`, `her`. 추가 엔진 코드 없음. (헤더에 게이지 바 노출은 추후.)
- **분노 = 도박의 동력**: 헌팅 실패·엄마와 싸움·포기가 anger를 쌓고, 임계 넘으면 몰빵→흙수저/파멸로 떠밀리는 설계. (임계 판정은 4~5주차 게이팅에서 구현 예정.)

## 4-2. 투자 처리 — A안 확정 (결정론, 결과는 인간관계 층에)

주식/코인/카지노는 **조작형 시스템이 아니다.** 매 턴 켜서 운용하는 앱/메뉴로 만들지 않는다.

- **A안 확정:** 투자/도박 결과는 **랜덤이 아니라 작가가 정한다(결정론).** 같은 베팅이라도 플레이어의 *선택(절제 vs 폭주, 엄마를 어떻게 대했나)*이 *어떤 정해진 결과로 갈지*를 가른다 → **결정론적 분기.** 선택은 살아있고 운(주사위)만 없앤 것.
- **B안(확률/랜덤)·C안(상시 폰 앱) = V1 금지.** B는 재미+스토리 밸런싱이 지옥이고, C는 상시 켜지는 앱 = 샌드박스 = 매번 못 끝낸 늪. 둘 다 V2 이후로도 신중히.
- **결과는 숫자가 아니라 인간관계/방 상태/엄마/거울에 남긴다.** `-8만원 알림`이 아니라 → *엄마 반찬이 굳어 있고 책상에 캔이 하나 늘었다.* 흔적은 **추상 금지, 구체적 물건 하나**로.
- 봉차트 프로토타입(`game/proto-market.html`)은 폐기 X — **가끔 목격되는 한 컷(연출)**으로 강등해 재사용.

> 핵심: A안 = "인간관계 길" 그 자체. 투자는 시스템이 아니라 인간관계 층에 떨어지는 결과 한 방울.

## 5. 원칙 (00_core와 일치하는 것만 유지)

- `[시스템]` 창 남발 금지, 게임 UI 덤프 금지 — 장면으로 번역.
- 한 화에 돈 한 칸 + 감정 한 칸. 보상은 다음 화로.
- 전 재산 올인 미화 금지 — 단, 이번 V1은 "충동의 대가"가 주제라 **올인의 결과(파멸/대박)를 엔딩으로** 보여줌.
- **작게 끝낸다.** 5주·4엔딩 안에서 완결. 콘텐츠 욕심은 V2.
